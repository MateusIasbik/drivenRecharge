export type PhoneData = {
    phone: string[],
    carrier: string,
    fullname: string,
    description: string,
    cpf: string
};

export type Phone = {
    id: number;
    phone: string[];
    fullname: string,
    description: string
}